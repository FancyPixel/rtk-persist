import { createListenerMiddleware } from "@reduxjs/toolkit";

/**
 * Global RTK listener middleware
 *
 * {@link https://redux-toolkit.js.org/api/createListenerMiddleware}
 *
 */
export const listenerMiddleware = createListenerMiddleware();
